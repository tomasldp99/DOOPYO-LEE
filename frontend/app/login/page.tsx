"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";
export default function Login(){const router=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await api<{access_token:string}>("/auth/login",{method:"POST",body:JSON.stringify(Object.fromEntries(f))});localStorage.setItem("edupay_token",r.access_token);router.push("/admin")}catch(x){setError(x instanceof Error?x.message:"로그인 실패")}}return <main className="grid min-h-screen place-items-center bg-[#edf5f1] p-5"><div className="w-full max-w-md"><div className="mb-7 text-center"><div className="mx-auto mb-3 grid h-12 w-12 place-items-center rounded-2xl bg-brand text-xl font-bold text-white">E</div><h1 className="text-2xl font-bold">EduPay Link</h1><p className="mt-1 text-sm text-slate-500">교육 운영자 관리자 로그인</p></div><form onSubmit={submit} className="card space-y-5 p-7"><label><span className="label">이메일</span><input className="field" name="email" type="email" defaultValue="admin@edupay.link" required/></label><label><span className="label">비밀번호</span><input className="field" name="password" type="password" placeholder="8자 이상" required/></label>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="btn btn-primary w-full">로그인</button><p className="text-center text-xs text-slate-400">관리자 계정은 환경설정에서 안전하게 관리됩니다.</p></form></div></main>}
