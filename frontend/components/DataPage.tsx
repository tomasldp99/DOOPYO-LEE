import AdminShell from "./AdminShell";
export default function DataPage({title,description,columns,rows}:{title:string;description:string;columns:string[];rows:string[][]}){return <AdminShell title={title}><div className="mb-5"><h2 className="text-2xl font-bold">{title}</h2><p className="mt-1 text-sm text-slate-500">{description}</p></div><div className="card overflow-hidden"><div className="flex gap-3 border-b p-4"><input aria-label="검색" className="field max-w-xs" placeholder="이름, 이메일 또는 교육명 검색"/><select aria-label="상태 필터" className="field max-w-36"><option>전체 상태</option><option>결제완료</option><option>미결제</option><option>환불</option></select><button className="btn ml-auto border bg-white text-sm">Excel 다운로드</button></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-mist text-xs text-slate-500"><tr>{columns.map(x=><th className="px-5 py-4" key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr className="border-t" key={i}>{r.map((x,j)=><td className="px-5 py-4" key={j}>{j===r.length-1?<span className={`badge ${x.includes("완료")||x.includes("발송")?"bg-emerald-50 text-emerald-700":x.includes("대기")||x.includes("미결제")?"bg-amber-50 text-amber-700":"bg-slate-100 text-slate-600"}`}>{x}</span>:x}</td>)}</tr>)}</tbody></table></div></div></AdminShell>}
